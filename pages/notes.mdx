import { NextSeo } from "next-seo";

WIP - Adding in my notes and learning on all the different topics I explore

export default ({ children }) => {
  return (
    <>
      <NextSeo title="Notes | Adam Majmudar" />
      {children}
    </>
  );
};
